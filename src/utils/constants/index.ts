export enum ELayoverOption {
  ALL = -1,
  NO_LAYOVERS = 0,
  ONE_LAYOVER = 1,
  TWO_LAYOVERS = 2,
  THREE_LAYOVERS = 3,
}

export enum ESortOption {
  PRICE = 'price',
  DURATION = 'duration',
}

export enum EAirline {
  S_7 = 's7',
}
