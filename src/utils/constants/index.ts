export enum ELayoverOption {
  NO_LAYOVERS = -1,
  ALL = 0,
  ONE_LAYOVER = 1,
  TWO_LAYOVERS = 2,
  THREE_LAYOVERS = 3,
}

export enum ESortOption {
  PRICE = 'price',
  DURATION = 'duration',
}

export enum EAirline {
  S_7 = 's7',
}
